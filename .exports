#!/usr/bin/env zsh

# Preferred editors.
export EDITOR='code --wait'
export GEM_EDITOR='code'

# Enable persistent REPL history for `node`.
export NODE_REPL_HISTORY="$HOME/.node_history"
export NODE_REPL_HISTORY_SIZE='32768'

# Make Python use UTF-8 encoding for input/output streams.
export PYTHONIOENCODING='UTF-8'

# Prefer US English and use UTF-8.
export LANG='en_US.UTF-8'
export LC_ALL='en_US.UTF-8'

# Highlight section titles in manual pages when possible.
if command -v tput >/dev/null 2>&1; then
	export LESS_TERMCAP_md="$(tput bold 2>/dev/null)$(tput setaf 3 2>/dev/null)"
fi

# Don’t clear the screen after quitting a manual page.
export MANPAGER='less -X'

# Homebrew preferences.
typeset -U path PATH
path=("$(brew --prefix)/sbin" $path)
export PATH
export HOMEBREW_CASK_OPTS='--appdir=/Applications'

# This keeps apple from throwing a forking error.
export OBJC_DISABLE_INITIALIZE_FORK_SAFETY=YES

# Avoid issues with `gpg` as installed via Homebrew.
# https://stackoverflow.com/a/42265848/96656
export GPG_TTY="$(tty)"
