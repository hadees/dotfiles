// Placeholder written by the public dotfiles only when this file is missing.
// The personal overlay replaces it with real rules (which name accounts and
// domains, so they cannot live in the public repo). See ~/.finicky.ts.
export const defaultBrowser = undefined;
export const rewrite = [];
export default [];
