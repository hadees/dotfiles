// Placeholder written by the public dotfiles only when this file is missing.
// The work overlay replaces it with real rules (which name orgs and domains,
// so they cannot live in the public repo). See ~/.finicky.ts.
export const rewrite = [];
export default [];
